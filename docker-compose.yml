services:
  rmq-mcp:
    build: .
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    environment:
      - RABBITMQ_MCP_TRANSPORT=http
      - RABBITMQ_MCP_HOST=0.0.0.0
      - RABBITMQ_MCP_PORT=9000
    ports:
      - "9000:9000"
    stdin_open: true
    tty: true
